version: '3.3'

networks:
  net:
    
services:
  web0:
    image: 'my_proxy:latest'
    container_name: web0
    ports:
      - '80:80'
      # - '8080:8080'
    entrypoint: /opt/runner.bash
    # logging:
    #   driver: "json-file"
    #   options:
    #     max-size: "10m"
    #     max-file: "5"
    networks: 
      net:
        aliases: 
          - web1
    # links:
    #   - web1

  web1:
    image: 'my_proxy:latest'
    container_name: web1
    ports:
      - 81:80
      # - 8081:8080
    entrypoint: /opt/runner.bash
    # logging:
    #   driver: "json-file"
    #   options:
    #     max-size: "10m"
    #     max-file: "5"
    networks: 
      net:
        aliases: 
          - web0
    # links:
    #   - web0